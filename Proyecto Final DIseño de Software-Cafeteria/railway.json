{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd cafeteria-orders-api && mvn clean package -DskipTests"
  },
  "deploy": {
    "startCommand": "cd cafeteria-orders-api && java -Dserver.port=$PORT -jar target/cafeteria-orders-api-0.0.1-SNAPSHOT.jar",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
